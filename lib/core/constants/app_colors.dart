import 'package:flutter/material.dart';

/// Define as cores principais utilizadas em todo o aplicativo Lakes Delivery.
class AppColors {
  /// A cor primária do aplicativo (Vermelho).
  static const Color primaryColor = Color(0xFFFF4929);

  /// A cor de fundo principal clara.
  static const Color backgroundLight = Color(0xFFF8F6F5);
}
